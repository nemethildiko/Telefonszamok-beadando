<Window x:Class="Telefonszamok_Alap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Telefonkönyv" Height="450" Width="900">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

       
        <DataGrid x:Name="dgSzemelyek"
                  Grid.Column="0"
                  AutoGenerateColumns="True"
                  SelectionChanged="dgSzemelyek_SelectionChanged"
                  Margin="10"/>

       
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

          
            <DataGrid x:Name="dgTelefonszamok"
                      Grid.Row="0"
                      AutoGenerateColumns="False"
                      Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding id}" />
                    <DataGridTextColumn Header="Telefonszám" Binding="{Binding Szam}" />
                </DataGrid.Columns>
            </DataGrid>


            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <TextBox x:Name="tbUjTelefonszam"
             Width="200"
             Margin="0,0,10,0"/>

                <Button Content="Hozzáadás"
            Width="100"
            Margin="0,0,10,0"
            Click="BtnTelefonszamHozzaadas_Click"/>

                <Button Content="Törlés"
            Width="100"
            Click="BtnTelefonszamTorles_Click"/>
            </StackPanel>

        </Grid>

    </Grid>
</Window>
